{
  "name": "tenantra",
  "version": "0.1.0",
  "description": "Tenantra monorepo for admin dashboard and future resident apps",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "prettier": "^3.1.0",
    "turbo": "^1.13.3"
  }
}
